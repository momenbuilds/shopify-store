<div style="max-width:900px;margin:40px auto;padding:0 24px"><h1 style="font-family:Georgia,serif;font-weight:400">{{ blog.title }}</h1>{% for article in blog.articles %}<h3><a href="{{ article.url }}">{{ article.title }}</a></h3>{% endfor %}</div>
